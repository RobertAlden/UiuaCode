# ComplexField takes a span, a resolution, and a complex offset
# and generates a 2d field of complex numbers
# such that it covers -span to span in both axes with a step size of resolution.
# and the origin at the offset.
ComplexField ← +⊞ℂ⇌.⊂⊂¯⇌,0+⟜⍜÷(⇡⁅)
