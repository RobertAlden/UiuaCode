Input ← 236491_713787
Range ← (+⊃(⊢)(⇡/-))
Digits ← (-@0⍘parse)
CondA ← (/↥≡/=◫2)
CondB ← (≍⊃(⍏|⇡⧻))
CondC ← (∊2⍘⊚⊛)
Silver ← (×⊃(CondA|CondB))
Gold ← (××⊃(CondA|CondB|CondC))
Solution ← (∩(⧻▽)⊙:≡(⊃(Silver|Gold) Digits)..Range Input)

Solution
