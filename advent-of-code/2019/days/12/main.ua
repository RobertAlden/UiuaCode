Gravity ← (⊙+:≡/++¯⊠⊃(<|>)..)
Velocity ← (+,)

Step ← (Velocity Gravity)
Energy ← ≡/+⌵

⊜(⊜(parse↘3)≠@,.↘¯1)≠@\n. &fras "input.txt"
:↯:0△.

Silver ← /+×∩Energy ⍥Step 1000
# Silver
# Gold ← ()

HashRow ← (◿9999×ⁿ:100+1⇡⧻.)
Hash ← (/+/+⌵HashRow≡HashRow≡⊂)
RunTillHashCollision ← (⍢(⊙Step ⊂⊙(Hash,,))(¬∊:⊙Hash) [])

⧻RunTillHashCollision
